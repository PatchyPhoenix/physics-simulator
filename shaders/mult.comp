#version 430

layout (local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

uniform double x;
uniform double y;

layout (std430, binding=2) buffer product {
    double r;
};

void main() {
    r = x * y;
}