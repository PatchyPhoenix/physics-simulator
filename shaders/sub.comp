#version 430 core

layout (local_size_x = 1, local_size_y = 2, local_size_z = 1) in;

uniform double x;
uniform double y;

layout (std430, binding = 1) buffer difference {
    double r;
};

void main() {
    r = x - y;
}